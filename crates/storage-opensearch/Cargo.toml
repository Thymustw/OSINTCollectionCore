[package]
name = "storage-opensearch"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license-file.workspace = true
publish.workspace = true
description = "OpenSearch SearchStore adapter 與 ml-commons EmbeddingProvider"

[dependencies]
async-trait = { workspace = true }
# ProjectionStore 的 checkpoint／lag 用 DateTime<Utc>（Phase 0f 起從 dev 移上來）。
chrono = { workspace = true }
opensearch = { workspace = true }
serde_json = { workspace = true }
storage-core = { workspace = true }
# ProjectionCheckpoint.last_object_id 是 ObjectId（= Uuid），要解析狀態列裡的字串。
uuid = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
