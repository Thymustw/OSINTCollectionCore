[package]
name = "core-model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license-file.workspace = true
publish.workspace = true
description = "V0.1 canonical domain types（serde）。欄位對齊 docs/specs/SPEC_V0.1.md"

[dependencies]
chrono = { workspace = true }
# content hash（SPEC §15 Stage 3）的定義放在 core-model，讓 normalizer 與 deduplicator
# 共用同一份實作。見 src/content.rs 的說明。
hex = { workspace = true }
serde = { workspace = true }
sha2 = { workspace = true }
serde_json = { workspace = true }
# canonical URL 正規化（SPEC §15 Stage 2）。放在 core-model 讓 deduplicator 與
# entity-worker 共用同一份規則，見 src/url_norm.rs 開頭的說明。
url = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
